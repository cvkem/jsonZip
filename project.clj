(defproject org.clojars.cvkem/jsonzip "1.0.1"
  :description "Toolset to traverse a json-file (clojure map) with clojure.zip. Compound structures are mapped to a vector with label 'jsonchildren' such that these can be editted."
  :dependencies [[org.clojure/clojure "1.3.0"]] 
  :dev-dependencies [[lein-marginalia "0.6.1"]])
